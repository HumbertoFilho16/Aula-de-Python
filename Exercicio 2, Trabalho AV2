def soma_imposto(taxa_imposto, custo):
  x = ((taxa_imposto / 100) * custo) + custo
  return x

print(soma_imposto(float(input('Insira a porcentagem do imposto: ')), float(input('Insira o custo: '))))
